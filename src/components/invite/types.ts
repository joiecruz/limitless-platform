export interface InviteFormData {
  firstName: string;
  lastName: string;
  password: string;
  role?: string;
  companySize?: string;
  referralSource?: string;
  goals?: string;
}

export interface UserData {
  firstName: string;
  lastName: string;
  role?: string;
  companySize?: string;
  referralSource?: string;
  goals?: string;
}