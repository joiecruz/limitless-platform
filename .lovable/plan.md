1. Update `DesignThinkingSection.tsx`: change `max-w-3xl mx-auto` to `max-w-7xl mx-auto` so it aligns with the rest of the page.

2. Update `BeliefSections.tsx`: change `max-w-5xl mx-auto` to `max-w-7xl mx-auto` in both sections so they share the same container width and left-edge alignment as the hero and "People We Help Evolve" sections.

3. Update `TestimonialsRailSection.tsx`: change the title container from centered to `max-w-7xl mx-auto` with left-aligned text to match page margins.

4. Update `AIHomepage.tsx` hero headline: adjust the hero `h1` styling so that "human-centered AI" stays on a single line (reduce base font size or use `whitespace-nowrap` on that span while slightly shrinking the overall headline scale).